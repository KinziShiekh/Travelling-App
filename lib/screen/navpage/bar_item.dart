import 'package:flutter/material.dart';

class BarItemsPage extends StatelessWidget {
  const BarItemsPage({super.key});

  @override
  Widget build(BuildContext context) {
    // ignore: avoid_unnecessary_containers
    return   Container(
      child: const Center(child: Text('BarItemPage')),
    );
  }
}